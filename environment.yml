name: multialign
channels:
        - conda-forge
        - bioconda
dependencies:
        - snakemake == 9.*
        - snakemake-executor-plugin-slurm
        - snakemake-storage-plugin-fs
        - mamba
